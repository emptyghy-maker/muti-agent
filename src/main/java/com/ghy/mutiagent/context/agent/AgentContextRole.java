package com.ghy.mutiagent.context.agent;

/** 角色级上下文边界；枚举值同时作为灰度配置名。 */
public enum AgentContextRole {
    PREFERENCE,
    REQUIREMENT,
    ATTRACTION,
    FOOD,
    HOTEL,
    PLANNING,
    REPAIR,
    PATCH
}
